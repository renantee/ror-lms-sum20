<h1 class="ui header">Users</h1>
<h2><%= @users.length <= 0 ? 'No Users' : "" %></h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.full_name, user %>
    </li>
  <% end %>
</ul>
